# class 파일은 제외
*.class

index.txt

# 확장자가 .a인 파일 무시
*.a

# 윗 라인에서 확장자가 a인 파일은 무시했지만 lib.a 는 무시하고 싶지 않을 경우
!lib.a

# 현재 디렉토리에 있는 TODO 파일은 무시하고 subdir/TODO 처럼 하위 디렉토리에 있는 파일은 무시 하지않음
/TODO

# build 디렉토리에 있는 모든 파일은 무시하겠음
build/

# doc 폴더 아래에 있는 모든 txt 파일을 무시하고 싶다면( doc/server/test.txt 경로의 파일은 무시하지 않음)
doc/*.txt

# doc 폴더 아래에 있는 모든 .pdf 파일 무시(doc/server/1.pdf 무시)
doc/**/*.pdf